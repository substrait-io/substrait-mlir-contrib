name: License headers

on: pull_request

jobs:
  license:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Run SPDX license checker
      uses: ingomueller-net/spdx@split-by-newline
      with:
        licenses: |-
          Apache-2.0 WITH LLVM-exception
